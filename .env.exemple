# PostgreSQL
POSTGRES_USER=tokenscope
POSTGRES_PASSWORD=tokenscope_dev_password
POSTGRES_DB=tokenscope
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Application ports
FRONTEND_PORT=5173
BACKEND_PORT=3000

# JWT authentication (development placeholder — never commit the real secret)
JWT_SECRET=dev-only-placeholder-secret-change-me
JWT_ISSUER=tokenscope
JWT_AUDIENCE=tokenscope-web
JWT_ACCESS_TTL_SECONDS=3600
